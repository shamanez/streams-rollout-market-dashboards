# 📦 Moved

The streams-rollout-market dashboards now live alongside the source code at

## **https://shamanez.github.io/streams-rollout-market/**

The source repo (previously private) is now public, so the snapshot
no longer needs its own home. This repo is kept around only as a
redirect for old links.
